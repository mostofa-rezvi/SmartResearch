# Known Issues & Technical Debt

- **Socket.IO reconnection sometimes misses events** – investigation pending.
- **Elasticsearch index rebuild takes 45 minutes** – need incremental updates.
- **Neo4j write latency under heavy load** – consider read replicas.