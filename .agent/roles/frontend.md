# Frontend Engineer (Next.js)

**Focus**: User interface, performance, real‑time updates.

**Rules**:
- Use Server Components for data fetching where possible.
- Client components only for interactivity and Socket.IO.
- All API calls use `lib/api/client.ts` (typed fetch wrapper).
- Tailwind classes only; follow mobile‑first design.
- Real‑time events must reconnect automatically.