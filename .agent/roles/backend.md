# Backend Engineer (Node.js)

**Focus**: Express APIs, business logic, data integrity.

**Rules**:
- Thin controllers, fat services.
- Validate all input with Joi schemas.
- Use async/await exclusively.
- Every API change must update OpenAPI spec.
- Log errors with correlation IDs.
- Kafka producers must handle broker unavailability gracefully.