# PLAN 09-02 — Wave 2: Hybrid RRF & Filtering

## Objective
Implement the full hybrid search query with RRF fusion and pre-filtering.

## Context & Constraints
- **Fusion**: Native RRF.
- **Latency**: < 200ms target.

## Tasks

### 9.4 Hybrid Search Implementation
- **Action**: Update `backend/src/services/discovery.service.js`.
- **Logic**: 
    1. Construct the combined `query` + `knn` + `rank` request.
    2. Pass filters into the `knn.filter` block.
- **Verification**: Search results show high-relevance matches for both keywords and semantics.

### 9.5 Search Benchmarking Script
- **Action**: Create `scripts/benchmark_search.py`.
- **Logic**: 
    1. Send 50 concurrent requests to `/api/v1/discovery/search`.
    2. Measure mean and p95 latency.
- **Verification**: Log output shows p95 < 200ms.

### 9.6 Search Result Highlighting
- **Action**: Update the response format.
- **Logic**: Add metadata about *why* it matched (BM25 score vs kNN score) for debugging.
- **Verification**: JSON response includes a `match_type` field.

## Verification
- Benchmark report matches targets.
