---
phase: 013-collaboration-workspace
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [
  "frontend/src/components/collaborative-editor.tsx",
  "frontend/src/components/live-cursors.tsx",
  "frontend/src/app/workspace/document/[id]/page.tsx"
]
autonomous: true
requirements: ["REQ-013-02", "REQ-013-03"]
user_setup: []
must_haves:
  truths:
    - "Tiptap editor is integrated with basic formatting"
    - "Yjs is mocked or prepared for WebRTC/WebSocket sync"
    - "Live cursors and presence rings use deterministic color mapping"
  artifacts:
    - path: "frontend/src/components/collaborative-editor.tsx"
      provides: "Rich text editor surface"
  key_links: []
---

<objective>
Implement the real-time collaborative document editor using Tiptap and prepare Yjs structures, complete with live presence indicators.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/013-collaboration-workspace/013-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Tiptap Collaborative Editor</name>
  <files>frontend/src/components/collaborative-editor.tsx</files>
  <read_first>.planning/phases/013-collaboration-workspace/013-UI-SPEC.md</read_first>
  <action>Create `collaborative-editor.tsx` wrapping Tiptap. Include a basic formatting toolbar (bold, italic, h1, h2). Set the background to Dominant (#FFFFFF / #0F172A). Document the required Yjs provider integration points, using a placeholder/mock provider for this UI phase if necessary.</action>
  <verify>Tiptap editor mounts and accepts rich text input.</verify>
  <acceptance_criteria>
    - Toolbar functions correctly for bold/italic.
    - Editor surface adheres to `lg` toolbar spacing scale.
  </acceptance_criteria>
  <done>Collaborative editor base implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Live Presence Indicators</name>
  <files>frontend/src/components/live-cursors.tsx</files>
  <read_first>frontend/src/components/collaborative-editor.tsx</read_first>
  <action>Create `live-cursors.tsx` to handle rendering custom colored avatar rings and mock floating cursors inside the document area. Assign deterministic colors (e.g., #EC4899, #8B5CF6) to mock connected users.</action>
  <verify>Mock cursors render with correct colors over the document.</verify>
  <acceptance_criteria>
    - Avatars feature presence rings.
    - Mock cursors are visible with proper user label coloring.
  </acceptance_criteria>
  <done>Presence indicators created.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble Document Workspace Route</name>
  <files>frontend/src/app/workspace/document/[id]/page.tsx</files>
  <read_first>frontend/src/components/collaborative-editor.tsx</read_first>
  <action>Create `frontend/src/app/workspace/document/[id]/page.tsx`. Integrate the `CollaborativeEditor` and `LiveCursors` components. Build a header that displays the active document title and connected user avatars.</action>
  <verify>Document page route compiles and renders the editor.</verify>
  <acceptance_criteria>
    - Route correctly mounts editor and presence UI.
  </acceptance_criteria>
  <done>Document workspace assembled.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Tiptap editor is interactive.
- [ ] Colored presence indicators visually stand out and don't conflict with text.
</verification>

<success_criteria>
- The collaborative editing foundation is fully built and matches the Phase 13 spec.
</success_criteria>

<output>
After completion, create `.planning/phases/013-collaboration-workspace/013-02-SUMMARY.md`
</output>
