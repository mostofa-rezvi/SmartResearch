---
phase: 013-collaboration-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/src/components/kanban-board.tsx",
  "frontend/src/components/timeline-view.tsx",
  "frontend/src/components/notifications-panel.tsx",
  "frontend/src/app/workspace/page.tsx"
]
autonomous: true
requirements: ["REQ-013-01", "REQ-013-04"]
user_setup: []
must_haves:
  truths:
    - "Kanban board supports columns and drag-and-drop or status changes"
    - "Notifications panel displays invites, mentions, and updates"
    - "Workspace dashboard successfully integrates kanban and timeline"
  artifacts:
    - path: "frontend/src/components/kanban-board.tsx"
      provides: "Task management interface"
    - path: "frontend/src/components/notifications-panel.tsx"
      provides: "User notification center"
  key_links: []
---

<objective>
Implement the primary Workspace layout including the Kanban Task Board, Milestone Timeline, and Notifications Panel.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/013-collaboration-workspace/013-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Kanban Task Board</name>
  <files>frontend/src/components/kanban-board.tsx</files>
  <read_first>.planning/phases/013-collaboration-workspace/013-UI-SPEC.md</read_first>
  <action>Create a `kanban-board.tsx` component. Setup standard columns: "To Do", "In Progress", "Review", "Done". Use shadcn/ui Card for the task items. Use standard spacing (`md` padding for columns, `sm` for card internals).</action>
  <verify>Kanban renders 4 columns with mock tasks without UI overflow.</verify>
  <acceptance_criteria>
    - Kanban columns are visible.
    - Tasks are correctly rendered inside columns.
  </acceptance_criteria>
  <done>Kanban board implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Build Notifications Panel</name>
  <files>frontend/src/components/notifications-panel.tsx</files>
  <read_first>.planning/phases/013-collaboration-workspace/013-UI-SPEC.md</read_first>
  <action>Create `notifications-panel.tsx`. Design it as a DropdownMenu or Popover. Use the Accent token (#3B82F6) for unread badges. Include mock items for invites, task updates, and mentions.</action>
  <verify>Panel opens and renders mock notifications correctly.</verify>
  <acceptance_criteria>
    - Popover triggers correctly.
    - Unread states use the designated Accent color.
  </acceptance_criteria>
  <done>Notifications panel completed.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold Workspace Dashboard</name>
  <files>frontend/src/app/workspace/page.tsx, frontend/src/components/timeline-view.tsx</files>
  <read_first>frontend/src/components/kanban-board.tsx</read_first>
  <action>Create a simple `timeline-view.tsx` mock component showing project phases. Then create `frontend/src/app/workspace/page.tsx`. Integrate the Kanban Board and Timeline. Ensure there's a header area to mount the Notifications panel.</action>
  <verify>Workspace route compiles and layout mounts successfully.</verify>
  <acceptance_criteria>
    - Workspace layout contains Timeline and Kanban board.
  </acceptance_criteria>
  <done>Workspace dashboard layout assembled.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Kanban layout is responsive and styled correctly.
- [ ] Notifications UI respects the design constraints and Accent tokens.
</verification>

<success_criteria>
- The project management tools of the workspace are fully scaffolded per Phase 13 UI spec.
</success_criteria>

<output>
After completion, create `.planning/phases/013-collaboration-workspace/013-01-SUMMARY.md`
</output>
