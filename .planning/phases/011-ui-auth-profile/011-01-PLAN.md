---
phase: 011-ui-auth-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/app/layout.tsx",
  "frontend/app/globals.css",
  "frontend/tailwind.config.ts",
  "frontend/app/(auth)/login/page.tsx",
  "frontend/app/(auth)/sign-up/page.tsx",
  "frontend/app/(auth)/verify/page.tsx",
  "frontend/app/api/auth/callback/route.ts"
]
autonomous: true
requirements: ["REQ-011-01", "REQ-011-02"]
user_setup: []
must_haves:
  truths:
    - "Tailwind and Next.js App Router are correctly configured"
    - "Users can navigate to login and sign up pages"
    - "OAuth callback endpoint exists"
  artifacts:
    - path: "frontend/app/layout.tsx"
      provides: "Root layout shell"
    - path: "frontend/app/(auth)/login/page.tsx"
      provides: "Login UI"
  key_links: []
---

<objective>
Scaffold the Next.js App Router, configure Tailwind CSS, create the layout shell, and build the Authentication UI.

Purpose: Establishes the frontend foundation and provides entry points for user authentication.
Output: Layout shell and functional auth pages.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/011-ui-auth-profile/011-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js App Router and Tailwind Configuration</name>
  <files>frontend/app/layout.tsx, frontend/app/globals.css, frontend/tailwind.config.ts</files>
  <read_first>.planning/phases/011-ui-auth-profile/011-UI-SPEC.md</read_first>
  <action>Initialize the root `layout.tsx` with Inter font from Google Fonts. Configure `tailwind.config.ts` with the color scale (Dominant: #0F172A, Accent: #3B82F6, Destructive: #EF4444). Set up `globals.css` with Tailwind directives.</action>
  <verify>npm run build passes without errors</verify>
  <acceptance_criteria>
    - frontend/tailwind.config.ts contains '#3B82F6'
    - frontend/app/layout.tsx contains 'Inter'
  </acceptance_criteria>
  <done>Tailwind and Layout configured</done>
</task>

<task type="auto">
  <name>Task 2: Build Auth Pages</name>
  <files>frontend/app/(auth)/login/page.tsx, frontend/app/(auth)/sign-up/page.tsx, frontend/app/(auth)/verify/page.tsx</files>
  <read_first>frontend/app/layout.tsx</read_first>
  <action>Create Next.js pages for Login, Sign-up, and Email Verification. Implement forms using shadcn/ui Card, Input, and Button components. Use the primary CTA copy specified in 011-UI-SPEC.md.</action>
  <verify>Pages render without Next.js errors</verify>
  <acceptance_criteria>
    - frontend/app/(auth)/login/page.tsx contains 'Sign In'
    - frontend/app/(auth)/sign-up/page.tsx contains 'Create Profile'
  </acceptance_criteria>
  <done>Auth pages are built</done>
</task>

<task type="auto">
  <name>Task 3: Implement OAuth Callback Endpoint</name>
  <files>frontend/app/api/auth/callback/route.ts</files>
  <read_first>frontend/app/(auth)/login/page.tsx</read_first>
  <action>Create a Next.js Route Handler for OAuth callback. It should receive the code from URL params and redirect to the profile page upon success.</action>
  <verify>Route handler compiles successfully</verify>
  <acceptance_criteria>
    - frontend/app/api/auth/callback/route.ts contains 'export async function GET'
  </acceptance_criteria>
  <done>OAuth callback handler implemented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Next.js build succeeds
- [ ] Auth pages are accessible
</verification>

<success_criteria>
- All tasks completed
- UI matches the UI-SPEC design contract
</success_criteria>

<output>
After completion, create `.planning/phases/011-ui-auth-profile/011-01-SUMMARY.md`
</output>
