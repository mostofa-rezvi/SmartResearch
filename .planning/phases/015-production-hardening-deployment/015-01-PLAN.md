---
wave: 1
depends_on: []
files_modified:
  - k8s/base/deployment.yaml
  - k8s/base/service.yaml
  - k8s/overlays/prod/hpa.yaml
  - scripts/setup-cloudflare.sh
autonomous: true
---

# Phase 15: Infrastructure & Config - Plan 1

<must_haves>
- Kubernetes manifests for all core services (frontend, backend, ml-service).
- HPA configuration targeting ML service pods with CPU/Memory thresholds.
- Cloudflare configuration script for CDN and WAF rules.
</must_haves>

<step>
<description>Create base Kubernetes manifests for deployments and services.</description>
<action>
Create `k8s/base/deployment.yaml` defining deployments for `frontend`, `backend`, and `ml-service`.
Create `k8s/base/service.yaml` defining cluster services.
</action>
<read_first>
- k8s/base/deployment.yaml
- k8s/base/service.yaml
</read_first>
<acceptance_criteria>
- `deployment.yaml` contains all 3 services.
- `service.yaml` contains correct port mappings.
</acceptance_criteria>
</step>

<step>
<description>Implement Horizontal Pod Autoscaling for ML services.</description>
<action>
Create `k8s/overlays/prod/hpa.yaml` with HPA rules for the `ml-service` (min: 2, max: 10, targetCPU: 70%).
</action>
<read_first>
- k8s/overlays/prod/hpa.yaml
</read_first>
<acceptance_criteria>
- `hpa.yaml` targets `ml-service`.
</acceptance_criteria>
</step>

<step>
<description>Create Cloudflare setup script.</description>
<action>
Create `scripts/setup-cloudflare.sh` with placeholders for API calls to set up CDN caching and WAF rules.
</action>
<read_first>
- scripts/setup-cloudflare.sh
</read_first>
<acceptance_criteria>
- Script is executable and contains WAF rule definitions.
</acceptance_criteria>
</step>

<verification>
- `kubectl diff -f k8s/base/` (mock/simulated)
- Script linting
</verification>
