---
wave: 2
depends_on: [015-01-PLAN.md]
files_modified:
  - tests/e2e/auth.spec.ts
  - tests/e2e/discovery.spec.ts
  - tests/load/benchmark.js
autonomous: true
---

# Phase 15: Quality & Benchmark - Plan 2

<must_haves>
- Playwright E2E tests covering Auth and Discovery flows.
- k6 load test script configured for 10k concurrent connections.
</must_haves>

<step>
<description>Implement E2E tests with Playwright.</description>
<action>
Create `tests/e2e/auth.spec.ts` for login/register flows.
Create `tests/e2e/discovery.spec.ts` for SBERT-powered search validation.
</action>
<read_first>
- tests/e2e/auth.spec.ts
- tests/e2e/discovery.spec.ts
</read_first>
<acceptance_criteria>
- Tests pass locally against dev environment.
</acceptance_criteria>
</step>

<step>
<description>Implement Load Testing with k6.</description>
<action>
Create `tests/load/benchmark.js` with k6 options for `vus: 10000` and `duration: '5m'`.
Add checks for HTTP 200 and response latency < 500ms.
</action>
<read_first>
- tests/load/benchmark.js
</read_first>
<acceptance_criteria>
- Script correctly initializes 10k VUs.
</acceptance_criteria>
</step>

<verification>
- `npx playwright test --list`
- `k6 inspect tests/load/benchmark.js`
</verification>
