---
phase: 012-discovery-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [
  "frontend/src/components/collaborator-card.tsx",
  "frontend/src/components/recommendation-feed.tsx",
  "frontend/src/app/discovery/page.tsx",
  "frontend/src/app/profile/[id]/page.tsx"
]
autonomous: true
requirements: ["REQ-012-03", "REQ-012-04"]
user_setup: []
must_haves:
  truths:
    - "Recommendation feed displays dynamic collaborator cards"
    - "Collaborator cards visually indicate similarity scores"
    - "Profile page enhanced with trust score and shared interests"
  artifacts:
    - path: "frontend/src/components/collaborator-card.tsx"
      provides: "Card UI for recommended researchers"
    - path: "frontend/src/components/recommendation-feed.tsx"
      provides: "Grid/list view of recommended peers"
  key_links: []
---

<objective>
Implement the Recommendation feed UI with similarity scores and enhance the Researcher profile page with trust scores and shared interests.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/012-discovery-dashboard/012-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Collaborator Card Component</name>
  <files>frontend/src/components/collaborator-card.tsx</files>
  <read_first>.planning/phases/012-discovery-dashboard/012-UI-SPEC.md</read_first>
  <action>Create `collaborator-card.tsx`. Use a Card to display researcher info (avatar, name, institution). Add a `Badge` to display the "Similarity Score" (e.g. 92% Match) using the Accent color. Provide a "Connect" primary CTA button.</action>
  <verify>Card visually matches UI spec and handles missing props gracefully.</verify>
  <acceptance_criteria>
    - Displays similarity score badge prominently.
    - Connect CTA is present.
  </acceptance_criteria>
  <done>Collaborator card component built.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble Recommendation Feed</name>
  <files>frontend/src/components/recommendation-feed.tsx, frontend/src/app/discovery/page.tsx</files>
  <read_first>frontend/src/components/collaborator-card.tsx</read_first>
  <action>Create `recommendation-feed.tsx` to render a grid/list of `CollaboratorCard`s. Update `frontend/src/app/discovery/page.tsx` to place the `RecommendationFeed` in the main content area alongside the `FilterSidebar`.</action>
  <verify>Discovery dashboard displays mock collaborator cards.</verify>
  <acceptance_criteria>
    - Feed is integrated into the Discovery page.
    - Feed layout adapts properly.
  </acceptance_criteria>
  <done>Recommendation feed implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Enhance Researcher Profile Page</name>
  <files>frontend/src/app/profile/[id]/page.tsx</files>
  <read_first>frontend/src/app/profile/[id]/page.tsx</read_first>
  <action>Modify the existing `profile/[id]/page.tsx`. Add UI elements for a "Trust Score" (TrustRank tier rendering) and a new section for "Shared Interests" to show overlapping domains with the current user.</action>
  <verify>Profile page renders the new sections correctly without breaking existing logic.</verify>
  <acceptance_criteria>
    - Trust Score / TrustRank tier is visible.
    - Shared interests section is added.
  </acceptance_criteria>
  <done>Profile page enhanced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Collaborator cards show similarity scores.
- [ ] Discovery page layout looks cohesive with sidebar, search, and feed.
- [ ] Profile page correctly displays Trust Score.
</verification>

<success_criteria>
- Recommendation and Discovery UX is fully assembled per the Phase 12 spec.
</success_criteria>

<output>
After completion, create `.planning/phases/012-discovery-dashboard/012-02-SUMMARY.md`
</output>
