---
phase: 012-discovery-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/src/components/search-bar.tsx",
  "frontend/src/components/filter-sidebar.tsx",
  "frontend/src/app/discovery/page.tsx",
  "frontend/src/app/api/search/suggestions/route.ts"
]
autonomous: true
requirements: ["REQ-012-01", "REQ-012-02"]
user_setup: []
must_haves:
  truths:
    - "Search bar implements debouncing for live suggestions"
    - "Filter sidebar exposes domain, skill, institution, and TrustRank tier filters"
    - "Discovery page layout integrates sidebar and search bar"
  artifacts:
    - path: "frontend/src/components/search-bar.tsx"
      provides: "Unified search input with suggestion dropdown"
    - path: "frontend/src/components/filter-sidebar.tsx"
      provides: "Interactive filtering UI"
  key_links: []
---

<objective>
Implement the unified search bar with debounced live suggestions and the filter sidebar for the Discovery Dashboard.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/012-discovery-dashboard/012-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Unified Search Bar Component</name>
  <files>frontend/src/components/search-bar.tsx, frontend/src/app/api/search/suggestions/route.ts</files>
  <read_first>.planning/phases/012-discovery-dashboard/012-UI-SPEC.md</read_first>
  <action>Create `search-bar.tsx` using shadcn/ui Input. Implement client-side debouncing (e.g., 300ms) for typed queries. Create a mock Next.js API route `api/search/suggestions/route.ts` that will eventually query Elasticsearch but for now returns mock suggestions based on query input. The search bar must display a dropdown with these suggestions.</action>
  <verify>API route returns JSON suggestions and search bar displays them without crashing.</verify>
  <acceptance_criteria>
    - Search input triggers debounced fetch calls.
    - Suggestions dropdown is visible and accessible.
  </acceptance_criteria>
  <done>Search bar and mock API route completed.</done>
</task>

<task type="auto">
  <name>Task 2: Build Filter Sidebar Component</name>
  <files>frontend/src/components/filter-sidebar.tsx</files>
  <read_first>.planning/phases/012-discovery-dashboard/012-UI-SPEC.md</read_first>
  <action>Create `filter-sidebar.tsx` with sections for: Domain (checkboxes), Skill (multi-select or checkboxes), Institution (text input), and TrustRank tier (Bronze/Silver/Gold toggles or slider). Adhere to the spacing scale (`xl` between sections) and use the standard secondary background color (#F8FAFC / #1E293B).</action>
  <verify>Sidebar renders correctly with responsive design considerations.</verify>
  <acceptance_criteria>
    - Contains filters for domain, skill, institution, and TrustRank.
  </acceptance_criteria>
  <done>Filter sidebar component implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble Discovery Layout</name>
  <files>frontend/src/app/discovery/page.tsx</files>
  <read_first>frontend/src/components/search-bar.tsx, frontend/src/components/filter-sidebar.tsx</read_first>
  <action>Scaffold the `frontend/src/app/discovery/page.tsx` integrating the `SearchBar` at the top and the `FilterSidebar` on the left (desktop). Leave the main content area empty for the recommendation feed.</action>
  <verify>Page compiles and displays the layout properly.</verify>
  <acceptance_criteria>
    - Discovery layout contains both the sidebar and search bar.
  </acceptance_criteria>
  <done>Discovery layout scaffolded.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Search bar debouncing logic works.
- [ ] Sidebar filters are interactive.
- [ ] Discovery page renders without errors.
</verification>

<success_criteria>
- Discovery layout foundation is established and visually adheres to the 012-UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/012-discovery-dashboard/012-01-SUMMARY.md`
</output>
