# Milestone v2.0 — Proposal Completion (all 10 modules → 100%)

**Goal:** Close every gap identified in `others/PROPOSAL-COMPLIANCE-CHECKLIST.md` so all 10 proposal modules reach full spec. Backend-first, real code + migrations + tests + live verification. Apache Kafka remains out of scope (Redis Streams is the async bus).

**Mode:** yolo · parallelization where file-safe · commit_docs. Conventions per `.planning/codebase/CONVENTIONS.md`.

**DB strategy:** one consolidated, idempotent migration `backend/migrations/v2_completion.sql` (all use `IF NOT EXISTS`). Apply to running Postgres + document for deploy.

---

## Phases (dependency order)

### Phase A — Module 1: Registration & Profile → 100%
- A1: Institutional email verification — `.edu`/`.ac.bd`/known-academic domains auto-set `is_institutional` + trust tier on register/verify.
- A2: `trust_tier` column + assignment logic (`unverified`→`basic`→`verified`→`professor`) in a `trust.service.js`; expose on profile.
- A3: Emit `profile.created` on the wired register path so ES + Neo4j sync fire; compute + store the 768-dim profile embedding in the ES `users` index.
- A4: Accept research interests/domain tags/skills at registration (optional arrays) → persist to junction tables.

### Phase B — Module 5: Forum + TrustRank → 100%
- B1: Threaded comments (`parent_id`) + accepted-answer flag on `community_posts`/`comments`.
- B2: TrustRank — create SUPPORTS/ENDORSES/CITES edges on vote/accept/endorse; schedule `graph_rank.py` (APScheduler); read `impactScore` back into `researcher_profiles`/reputation; expose on profile & feed. Professor role weight in ranking.
- B3: Semantic embedding of forum posts into an ES `posts` index (same space as profiles).

### Phase C — Module 6: Mentorship → 100%
- C1: Mentorship slots entity (`mentorship_slots`: domain, capacity, availability).
- C2: CF+CB pairing endpoint (reuse recommender / domain overlap) filtered by domain, availability, history.
- C3: Session tracking (`mentorship_sessions`: scheduled/completed, notes).
- C4: Reputation award on accepted mentorship + completed session.

### Phase D — Module 7: Publication Assistant → 100%
- D1: Wire the Scimago finder to the real `journals` table (405K rows) instead of the hardcoded 12-seed; abstract/topic + impact-factor ranking.
- D2: Live DOAJ query fallback for open-access journals.

### Phase E — Module 8: Notifications → 100%
- E1: New notification types: collaborator-match, workspace-activity, publication-update — emitted at the right call sites.
- E2: Bridge Socket.IO `notification:new` to the frontend bell (real-time, replace polling reliance).

### Phase F — Module 9: Profile & Credential Dashboard → 100%
- F1: Public profile: collaborations formed + mentorship history sections.
- F2: Public (opt-in) achievements/credential view; authored-papers list.

### Phase G — Module 10: Admin & Analytics → 100%
- G1: Trust-tier management admin endpoints (list/set tier).
- G2: Institutional-badge verification admin endpoints (verify/revoke).
- G3: Real match-quality wired to ML scores where available.

### Phase H — Module 4: Knowledge Library → 100%
- H1: `library_items` entity (paper/dataset/note/lit-review types).
- H2: PDF storage to MinIO/S3 (persist uploaded PDFs, not in-memory only).
- H3: ES `papers`/library indexing + real semantic library search. Feed recommender (embed abstracts).

### Phase I — Module 2: Matching Engine → 100%
- I1: Unified feed — merge papers + open projects into `/recommendations` (index + hydrate).
- I2: Embed paper abstracts into the profiles/papers vector space.

### Phase J — Module 3: Collaboration Workspace → 100%
- J1: Align real-time co-editor protocol (frontend `y-socket.io` ↔ backend) OR standardize on the backend custom Yjs protocol end-to-end.
- J2: Project selector (remove hardcoded projectId=1); tasks CRUD API backing the kanban.

### Phase K — Infrastructure hardening
- K1: `REDIS_URL` config fallback to `REDIS_HOST`/`PORT`; add to `.env`.
- K2: K8s port fixes (backend 5000, ML 8000) + secrets/ingress notes.
- K3: Input sanitization (sanitize-html/xss) on user content.
- K4: `/metrics` Prometheus exporter; confirm `openapi.yaml`.

---

## Verification per phase
- Unit tests (jest/pytest) green.
- Live smoke test against the running Docker stack (all datastores + backend + ML).
- Update `others/PROPOSAL-COMPLIANCE-CHECKLIST.md` module % on completion.
