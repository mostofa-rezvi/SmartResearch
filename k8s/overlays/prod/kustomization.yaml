apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Production overlay — inherits the base and adds horizontal autoscaling.
# Apply with:  kubectl apply -k k8s/overlays/prod
namespace: researchbridge

resources:
  - ../../base
  - hpa.yaml
